const Member = ({ member }) => {
    return (
        <div>
            <div>
                <h1>Hello.</h1>
            </div>
        {/* <h1>{member.name}</h1>
        <p>{member.email}</p>
        <p>{member.status}</p> */}
        </div>
    );

}

export default Member;